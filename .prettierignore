dist
node_modules
coverage
src/generated
# Owned by sub-agent-orchestrator/openapi/public-api-v1.ts and emitted via
# `pnpm openapi:generate` upstream. Different YAML quoting conventions than
# Prettier — keep the generated file byte-identical with the upstream source.
openapi/public-api-v1.yaml
bun.lock
bun.lockb
LICENSE
