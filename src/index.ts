/**
 * @brew/api — Official TypeScript SDK for the Brew public API.
 *
 * This is the single public entrypoint for the SDK. Everything that is part
 * of the public surface must be re-exported from here.
 *
 * The real client, resources, and errors will land in the upcoming TDD work.
 * For now this file exists so the scaffold compiles and bundles cleanly.
 */

export const SDK_NAME = 'brew-typescript-sdk'
export const SDK_VERSION = '0.0.0'
